python
python script.py                    # Run a file
python script.py arg1 arg2          # Pass arguments
python -c "print('Hello')"          # Run code directly
python -m module_name               # Run module as script
exit()
